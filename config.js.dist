let config = {};

config.api = {
    key: 'api_key',
    secret: 'api_secret',
    passphrase: 'api_passphrase',
};

config.product = {
    id: 'BTC-EUR',
    base_currency: "BTC",
    quote_currency: "EUR",
    base_min_size: "0.01",
    base_max_size: "10000.00",
    quote_increment: "0.01"
};

config.trade = {
    interval: 3600 // 1h
};
module.exports = config;